
      {/* {location.pathname.includes('/user') && activeStepIndex === 0 && (
        <ModalAddNoticeAddMyPetFirstStep
          setStepIndex={setActiveStepIndex}
          formData={formData}
          onClose={onClose}
        />
      )}
      {location.pathname.includes('/user') && activeStepIndex === 1 && (
        <ModalAddNoticeAddMySecondStep
          setStepIndex={setActiveStepIndex}
          formData={formData}
          onClose={onClose}
        />
      )} */}